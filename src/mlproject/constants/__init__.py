from pathlib import Path

CONFIG_FILE_PATH = Path("config/config.yaml")#in your current project directory look for subdirectory(folder) named config
PARAMS_FILE_PATH = Path("params.yaml")#in your root directory look for params.yaml
SCHEMA_FILE_PATH = Path("schema.yaml")